export default {
  PRODUCTS(state) {
    return state.products
  },
  URLS(state) {
    return state.urls
  },
  CURRENT_URL(state) {
    return state.current_url
  },
  IS_MOBILE(state) {
    return state.isMobile;
  },
  IS_DESKTOP(state) {
    return state.isDesktop;
  }
}
